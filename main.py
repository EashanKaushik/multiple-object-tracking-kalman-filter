
import numpy as np

# X_hat = np.array([[0], [1], [2], [3]])

# print(X_hat[1][0])

# B_t = np.array([[10], [20], [30], [40]])

# print((B_t.dot(1).reshape(B_t.shape[0], -1)))

H = np.zeros((1, 2))
H[0, 0] = 1

print(H)
